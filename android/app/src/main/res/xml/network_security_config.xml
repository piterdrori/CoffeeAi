<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Local backend uses HTTP (emulator 10.0.2.2, LAN IP, localhost). -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
