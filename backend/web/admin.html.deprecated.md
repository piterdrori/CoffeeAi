"""DEPRECATED — Stage 6A-1.

This legacy public admin UI is no longer served by any route.
The Control Center lives at /admin (session-protected) and /admin/login.

Do not restore a public route to this file. Do not store API_KEY in localStorage.
Kept only so historical references and security tests can assert it is inactive.
"""
